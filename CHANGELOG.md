# 변경 이력

## 2026-03-25
- GitHub Pages 검증 실패 원인이던 `_tabs/categories.md`, `_tabs/tags.md` 누락을 복원했습니다.
- 카테고리/태그 상세 페이지에서 상위 `/categories/`, `/tags/` 링크가 깨지던 문제를 해결했습니다.
- 배포 오류 원인과 수정 내용은 `CHANGELOG.md` 기준으로 함께 정리했습니다.
- 로컬 `htmlproofer`는 Windows `libcurl` DLL 누락으로 실행되지 않아, 생성된 `_site` 결과를 수동 확인했습니다.
- 사이드바에서는 `분류` 탭만 남기고, 중복되던 `카테고리`/`태그` 탭은 숨겼습니다. 실제 `/categories/`, `/tags/` 페이지는 유지됩니다.
- 포스트 본문 하단 왼쪽 카드와 하단 고정바 왼쪽 라벨을 `인기글`에서 `관련글`로 변경했습니다.
- 하단 왼쪽 추천 로직을 조회수 기반에서 실제 관련도 기반으로 변경했습니다.
- 관련글 슬롯은 항상 유지하되, 실제 관련글이 있으면 우선 사용하고 없으면 일반 글 fallback으로 채우도록 정리했습니다.
- 댓글 시스템은 최종적으로 다시 `Disqus`로 복원했습니다.
- 포스트 hero 메타뿐 아니라 본문 시작부 메타 줄에도 조회수가 표시되도록 보완했습니다.
- 편집기 배포 버튼이 원격 변경을 자동 동기화한 뒤 푸시하도록 개선했습니다.
- 로컬 전용 `.claude` / 로그 파일이 배포 커밋에 섞이지 않도록 자동 제외했습니다.
- 실수로 `/posts/7/`로 밀려난 글을 다시 `/posts/2/`로 원복하고, 파일명도 `_posts/2026-03-13-2.md`로 다시 맞췄습니다.
- 기존 글 저장 시 숨겨진 permalink 값이 비어도 원래 파일 번호를 우선 이어받도록 보완해, 편집 중 `2 -> 7`처럼 번호가 튀는 문제를 줄였습니다.
- 편집기 배포 시 `.claude` 같은 로컬 전용 파일만 남아 있으면 `git commit`을 생략하고, 실제 스테이징된 변경이 있을 때만 커밋하도록 수정했습니다.
- 편집기 시작 전 자동 동기화도 로컬 전용 파일만 바뀐 경우에는 계속 시도하도록 완화했습니다.
- 포스트 hero 메타와 본문 시작 메타가 연달아 중복 표시되던 문제를 정리하기 위해, 본문 상단 메타 줄을 제거했습니다.
- hero 제목과 설명 사이 간격을 조금 넓혀 설명이 더 자연스럽게 아래로 내려오도록 조정했습니다.
- 이미 정상 실행 중인 편집기가 있을 때 새 실행이 기존 프로세스를 죽이지 않도록 보완해, 편집기 실행 시 창/탭이 여러 개 뜨는 문제를 줄였습니다.
- 상단 고정바 높이를 아주 소폭 키워 버튼이 덜 답답하게 보이도록 조정했습니다.
- hero 우측 상단 액션 버튼도 상단바와 같은 순서로 재배치하고, 공유 버튼이 항상 맨 오른쪽에 오도록 맞췄습니다.
- hero 설명 문장은 제목보다 살짝 옅은 톤으로 낮춰, 제목과 시각적으로 더 잘 구분되도록 조정했습니다.
- 글 하단 프로필 이미지는 이미지 팝업 대상에서 제외하고 홈 링크만 남기도록 바꿔, 클릭 시 홈으로 안정적으로 이동하게 수정했습니다.
- 협업 기록 파일은 `AGENT_COLLAB_LOG.md`를 없애고 `CHANGELOG.md` 하나로 통일했습니다.
